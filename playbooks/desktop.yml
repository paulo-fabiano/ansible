- name: Configure Basic Services on Ubuntu Desktop
  hosts: local
  gather_facts: true
  vars:
    user: paulo # My name ;-)
  roles:
    - common
    - desktop

  # vars_prompt:
  #   - name: ansible_become_password
  #     prompt: "sudo password"
  #     private: true